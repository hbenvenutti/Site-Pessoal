import styled from 'styled-components';

// * ------------------------------------------------------------------------------------------ * //

export const CertificatesPageWrapper = styled.main`
  display: flex;
  width: 100%;
  flex-direction: column;
  gap: 2rem;

  img {
    width: 31.25rem;
    height: auto;
  }

  .certificate {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
`;
